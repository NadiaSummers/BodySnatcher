#include "enemy.h"

bool enemy::loadModel(string fileName){
	//load model from file
	
	return true;
}

void enemy::render(){
	//call functions in an abstract graphics class
}

void enemy::onCollision(gameObject &collisionObject){
	//code that performs actions in reponse to a collision
}