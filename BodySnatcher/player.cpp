#include "player.h"

bool player::loadModel(string fileName){
	//load model from file
	
	return true;
}

void player::render(){
	//call functions in an abstract graphics class
}

void player::onCollision(gameObject &collisionObject){
	//code that performs actions in reponse to a collision
}