--terrain.lua
--generation calls for world building
--should probably be in a larger 'level' lua file later on
--possible to call a constructor from here?

cpp_generateTerrain("textures/test-heightmap.raw", 128, 0.5);
